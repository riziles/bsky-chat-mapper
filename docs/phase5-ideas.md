- definitely need more options for date filter, or maybe just make it fully
  dynamic so user can just specify x days?
- if the messages are getting stored to user's index db, can we make it so
  subsequent pulls only pull new messages?
- can we make it so the steps (i.e. pull messages, generate embedding, etc) all
  just happen automatically without the user having to push a button in between
  each step? I love the progress bars but let's just have each one appended to
  dom if possible so user can see all of them?
- after messages have been grouped, is there a way to come up with better names
  for the chat?
- can we add an an option to display the filtered messages if a user selects one
  in the force graph?
- can we make the force graph nodes draggable?
- let's add a license file that we make sure is compliant with underlying
  packages
- since we are running this through an embedding model, can we build a semantic
  search function?
